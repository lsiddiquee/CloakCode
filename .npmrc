; Resolve all packages through the Microsoft package-feed proxy instead of the
; public npm registry. It screens for supply-chain / 0-day-vulnerable packages
; before they enter the build. Mirror this in the global npm/pnpm registry on
; each machine (see the README "Development" section).
;
; NB: package.json still pins pnpm@11.10.0 via `packageManager`, but the proxy
; can't serve pnpm's own tarball in the integrity-only form pnpm needs, so
; `pmOnFail: ignore` in pnpm-workspace.yaml makes pnpm use the installed pnpm
; instead of self-provisioning. (Only registry + auth are read from .npmrc.)
registry=https://packagefeedproxy.microsoft.io/npm/
