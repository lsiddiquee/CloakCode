{
  "name": "@cloakcode/agent",
  "version": "0.0.0",
  "private": true,
  "description": "The pausable tool-calling + confirmation loop. Pure TS; drives vscode.lm via an injected model port.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  }
}
