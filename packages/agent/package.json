{
  "name": "@cloakcode/agent",
  "version": "0.0.0",
  "private": true,
  "description": "The pausable tool-calling + confirmation loop. Pure TS; drives vscode.lm via an injected model port.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo '@cloakcode/agent: not started yet (actuator, I4) — no-op'",
    "typecheck": "echo '@cloakcode/agent: not started yet (actuator, I4) — no-op'",
    "lint": "echo '@cloakcode/agent: not started yet (actuator, I4) — no-op'",
    "test": "echo '@cloakcode/agent: not started yet (actuator, I4) — no-op'"
  }
}
