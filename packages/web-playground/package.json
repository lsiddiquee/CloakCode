{
  "name": "@cloakcode/web-playground",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only UI playground: renders the REAL @cloakcode/web App against an in-browser fake bridge + fixtures, so the PWA can be designed with no gateway. Never shipped; the dependency edge points playground → web only, so nothing here can reach the web build.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@cloakcode/protocol": "workspace:*",
    "@cloakcode/web": "workspace:*",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.0",
    "typescript": "^5.6.0",
    "vite": "8.1.5"
  }
}
